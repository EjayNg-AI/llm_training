[pytest]
testpaths = tests
markers =
  integration: End-to-end tokenizer training and export checks.
  determinism: Determinism and reproducibility checks.
  recovery: Resume and crash-recovery checks.
  slow: Slower tests intended for extended validation runs.
