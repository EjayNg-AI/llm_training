run:
  output_dir: "artifacts/runs/04_tokenize_corpus_owt_train"
  log_level: "INFO"
  structured_logs: true

artifacts_root: "artifacts"

input:
  corpus_id: "dedup_owt_train_txt"
  tokenizer_id: "tokenizer_owt_train_bpe_50k"
  corpus_type: "corpus_dedup"

output:
  docs_per_shard: 10000
  token_dtype: "uint32"
  append_eos: true
  artifact_id: "tokens_owt_train_bpe_50k"
