run:
  output_dir: "artifacts/runs/06_pretrain_owt_train"
  log_level: "INFO"
  structured_logs: true

artifacts_root: "artifacts"

input:
  packed_id: "packed_owt_train_bpe_50k_seq256"
  tokenizer_id: "tokenizer_owt_train_bpe_50k"

model:
  n_embd: 384
  n_layer: 6
  n_head: 6

training:
  seed: 0
  max_steps: 200
  batch_size: 8
  learning_rate: 0.0003
  weight_decay: 0.01
  warmup_steps: 20
  eval_every: 50
  eval_batches: 10
  save_every: 50
  artifact_id: "model_owt_train_bpe_50k_tinygpt"
